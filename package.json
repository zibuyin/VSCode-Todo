{
  "name": "vscode-todo-list-nathanyin",
  "displayName": "VSCode TODO List",
  "description": "A simple todo for VSCode",
  "version": "0.0.1",
  "publisher": "NathanYin",
  "engines": {
    "vscode": "^1.116.0"
  },
  "categories": [
    "Other"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/zibuyin/VSCode-Todo.git"
  },
  "homepage": "https://github.com/zibuyin/VSCode-Todo",
  "bugs": {
    "url": "https://github.com/zibuyin/VSCode-Todo/issues"
  },
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "vscode-todo.addNew",
        "title": "Add new TODO item"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "vscode-todo",
          "title": "Todo",
          "icon": "media/todo-view.svg"
        }
      ]
    },
    "views": {
      "vscode-todo": [
        {
          "id": "vscode-todo.todos",
          "name": "Todos",
          "icon": "media/todo-view.svg"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run check-types && npm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "npm run check-types && npm run lint && node esbuild.js --production",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@types/vscode": "^1.116.0",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2",
    "esbuild": "^0.27.3",
    "eslint": "^9.39.3",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.56.1"
  }
}